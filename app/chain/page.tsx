import {recentChain} from '@/lib/server/live-views';
export const dynamic='force-dynamic';
async function rpcStatus(){const rpc=(process.env.STRATUM_CHAIN_RPC_URL||'').replace(/\/$/,'');if(!rpc)return{connected:false,chainId:process.env.STRATUM_CHAIN_ID||'stratum-devnet-1',height:0,latestBlockHash:'Not connected',validators:[],quorumPower:0,nativeDenom:'ustrm',displayDenom:'STRM'};try{const r=await fetch(`${rpc}/v1/status`,{cache:'no-store'});if(!r.ok)throw new Error();return{...await r.json(),connected:true}}catch{return{connected:false,chainId:process.env.STRATUM_CHAIN_ID||'stratum-devnet-1',height:0,latestBlockHash:'RPC unavailable',validators:[],quorumPower:0,nativeDenom:'ustrm',displayDenom:'STRM'}}}
const short=(s:string|null,n=15)=>s?s.length>n*2?`${s.slice(0,n)}…${s.slice(-n)}`:s:'—';
export default async function ChainPage(){const [s,db]=await Promise.all([rpcStatus(),recentChain()]) as any;const height=Number(db.state?.height||s.height||0);return <><div className="page-head"><div><div className="eyebrow">Sovereign Trust Layer · Live</div><h1 className="title">STRATUM Chain</h1><div className="subtitle">Independent validator consensus for approved STRATUM Verified lifecycle proofs. The explorer below is reading finalized records from the live chain ledger.</div></div><span className={s.connected?'badge':'pending'}>{s.connected?'LIVE DEVNET':'RPC OFFLINE'}</span></div><div className="grid kpis"><div className="card"><div className="label">Chain</div><div className="metric" style={{fontSize:20}}>{s.chainId||db.state?.chain_id}</div></div><div className="card"><div className="label">Finalized Height</div><div className="metric">{height}</div></div><div className="card"><div className="label">Validators</div><div className="metric">{s.validators?.length||0}</div></div><div className="card"><div className="label">Native Asset</div><div className="metric">{s.displayDenom||'STRM'}</div></div></div><div className="grid two"><div className="card"><div className="section-head"><div><div className="label">Consensus State</div><h2>Latest finalized block</h2></div><span className="proof">{height>0?'QUORUM FINALIZED':'GENESIS'}</span></div><div className="verify-grid"><span>Block height</span><b>{height}</b><span>Block hash</span><b className="mono">{short(db.state?.latest_block_hash||s.latestBlockHash,22)}</b><span>Genesis</span><b className="mono">{short(db.state?.genesis_hash,22)}</b><span>Required power</span><b>{s.quorumPower||2} of 3</b></div></div><div className="card"><div className="label">Validator Authorities</div><h2>Network signers</h2><div className="timeline">{(s.validators||[]).map((v:any)=><div className="event" key={v.address}><i className="event-icon">✓</i><div><strong>{v.name}</strong><small className="mono">{v.address}</small></div></div>)}</div></div></div><div className="card table-card" style={{marginTop:16}}><div className="section-head"><div><div className="eyebrow">Block Explorer</div><h2>Finalized proof records</h2></div><span className="proof">{db.blocks.length} shown</span></div><table className="table"><thead><tr><th>Block</th><th>Event / Record</th><th>Transaction</th><th>Evidence</th><th>Proposer</th><th>Votes</th></tr></thead><tbody>{db.blocks.map((b:any)=><tr key={b.block_hash}><td><strong>#{b.height}</strong><div className="muted">{new Date(b.finalized_at).toLocaleString()}</div></td><td>{b.event_type||'Proof'}<div className="muted mono">{short(b.record_id,12)}</div></td><td className="mono" title={b.tx_hash}>{short(b.tx_hash,12)}</td><td className="mono" title={b.evidence_hash||''}>{short(b.evidence_hash,12)}</td><td>{b.proposer_validator_id}</td><td><span className="proof">{Array.isArray(b.votes_json)?b.votes_json.length:0}/3</span></td></tr>)}{!db.blocks.length&&<tr><td colSpan={6}><span className="muted">No finalized blocks beyond genesis yet.</span></td></tr>}</tbody></table></div><div className="card" style={{marginTop:16}}><div className="label">Trust Boundary</div><h2>Private evidence stays off-chain</h2><div className="subtitle">The ledger stores proof identifiers, event type, evidence-package SHA-256, canonical payload SHA-256, signer reference and timestamp. Photos, drawings, contracts and customer files remain in private application storage.</div></div></>}
