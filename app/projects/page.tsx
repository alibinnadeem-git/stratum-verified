import {projects} from '@/lib/data';export default function Projects(){return <><div className="page-head"><div><div className="eyebrow">Portfolio</div><h1 className="title">Projects</h1><p className="subtitle">Roll asset verification up from equipment to systems, sites and complete project turnover.</p></div><button className="action">+ New project</button></div><div className="project-grid">{projects.map(p=><div className="card project-card" key={p.id}><div className="project-status">{p.status}</div><div className="eyebrow">{p.id}</div><h2>{p.name}</h2><p className="muted">{p.client}<br/>{p.location}</p><div className="progress"><i style={{width:`${p.progress}%`}}/></div><div className="project-kpis"><div><strong>{p.progress}%</strong><span>Project</span></div><div><strong>{p.assets}</strong><span>Assets</span></div><div><strong>{p.verified}</strong><span>Verified</span></div></div></div>)}</div></>}