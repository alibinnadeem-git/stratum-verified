import Link from 'next/link';import {assets} from '@/lib/data';
export default function Assets(){return <><div className="page-head"><div><div className="eyebrow">Asset Registry</div><h1 className="title">Asset passports</h1><p className="subtitle">Durable identities linking physical equipment to provenance, operational evidence and immutable proof.</p></div><button className="action">+ Register asset</button></div><div className="card table-card"><table className="table"><thead><tr><th>Asset</th><th>Manufacturer / Serial</th><th>System / Location</th><th>Lifecycle</th><th>Health</th><th>Proof</th></tr></thead><tbody>{assets.map(a=><tr key={a.id}><td><Link href={`/assets/${a.id}`}><strong>{a.name}</strong></Link><div className="muted">{a.id} · {a.type}</div></td><td>{a.manufacturer}<div className="muted mono">{a.serial}</div></td><td>{a.system}<div className="muted">{a.location}</div></td><td><span className="status-chip">{a.status}</span></td><td><span className="status"><i className={'dot '+(a.health==='Healthy'?'':'warn')}/>{a.health}</span></td><td>{a.block?<span className="proof">✓ #{a.block}</span>:<span className="pending">Pending</span>}</td></tr>)}</tbody></table></div></>}