import {NextResponse} from 'next/server';import {query} from '@/lib/server/db';export async function GET(req:Request){try{const q=new URL(req.url).searchParams.get('q')?.trim();if(!q)return NextResponse.json({error:'q is required'},{status:400});const r=await query<any>(`SELECT le.id record_id,le.event_type,le.occurred_at,le.status,le.payload_sha256,le.evidence_package_sha256,le.ledger_network,le.ledger_tx_hash,le.ledger_block_height,a.asset_code,a.name asset_name,a.serial_number,p.project_code,p.name project_name FROM lifecycle_events le JOIN assets a ON a.id=le.asset_id JOIN projects p ON p.id=le.project_id LEFT JOIN evidence ev ON ev.lifecycle_event_id=le.id WHERE le.status='VERIFIED' AND (le.id::text=$1 OR le.payload_sha256=$1 OR le.evidence_package_sha256=$1 OR le.ledger_tx_hash=$1 OR a.asset_code=$1 OR a.serial_number=$1 OR a.qr_token::text=$1 OR ev.sha256=$1) ORDER BY le.occurred_at DESC LIMIT 25`,[q]);return NextResponse.json({verified:(r.rowCount??0)>0,records:r.rows})}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}