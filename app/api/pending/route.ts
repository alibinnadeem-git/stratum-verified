import {NextResponse} from 'next/server';
import {requireSession} from '@/lib/server/auth';
import {query} from '@/lib/server/db';
export async function GET(){try{const s=await requireSession();const r=await query(`SELECT le.id,le.project_id,le.asset_id,le.event_type,le.status,le.occurred_at,le.payload_sha256,le.performed_by,a.asset_code,a.name asset_name,p.project_code,p.name project_name,u.display_name performer_name,u.email performer_email,(le.performed_by=$2) performed_by_me,(SELECT COUNT(*)::int FROM evidence e WHERE e.lifecycle_event_id=le.id) evidence_count FROM lifecycle_events le JOIN assets a ON a.id=le.asset_id JOIN projects p ON p.id=le.project_id LEFT JOIN users u ON u.id=le.performed_by WHERE le.organization_id=$1 AND le.status='SUBMITTED' ORDER BY le.occurred_at DESC LIMIT 200`,[s.organizationId,s.userId]);return NextResponse.json({items:r.rows,session:{userId:s.userId,email:s.email,role:s.role}})}catch(e:any){return NextResponse.json({error:e.message},{status:e.status||500})}}