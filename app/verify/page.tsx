import VerifyClient from '@/components/VerifyClient';export default async function Verify({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=''}=await searchParams;return <><div className="eyebrow">Public Verification</div><h1 className="title">Verify without trusting us.</h1><p className="subtitle">Search a verified asset, serial, transaction, record ID, QR token or SHA-256 fingerprint. Private files can be hashed locally without uploading them.</p><VerifyClient initial={q}/></>}