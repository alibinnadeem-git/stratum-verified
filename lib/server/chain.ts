import {MockStratumDevnetAdapter,LedgerAdapter,LedgerRecord,AnchorReceipt} from '../ledger';class StratumChainRpcAdapter implements LedgerAdapter{constructor(private rpc:string,private chainId:string,private apiKey?:string){}async anchor(record:LedgerRecord):Promise<AnchorReceipt>{const res=await fetch(`${this.rpc.replace(/\/$/,'')}/stratum/verified/v1/records`,{method:'POST',headers:{'content-type':'application/json',...(this.apiKey?{'authorization':`Bearer ${this.apiKey}`}:{})},body:JSON.stringify(record),cache:'no-store'});if(!res.ok)throw new Error(`STRATUM Chain anchor failed: ${res.status}`);const j=await res.json() as {txHash:string;blockHeight:number;timestamp:string};return{network:this.chainId,txHash:j.txHash,blockHeight:j.blockHeight,timestamp:j.timestamp}}async verify(recordId:string,evidenceHash:string){const u=new URL(`${this.rpc.replace(/\/$/,'')}/stratum/verified/v1/records/${encodeURIComponent(recordId)}`);u.searchParams.set('evidenceHash',evidenceHash);const r=await fetch(u,{headers:this.apiKey?{'authorization':`Bearer ${this.apiKey}`}:{},cache:'no-store'});if(r.status===404)return{valid:false,network:this.chainId};if(!r.ok)throw new Error(`STRATUM Chain verify failed: ${r.status}`);const j=await r.json() as {valid:boolean;blockHeight?:number;txHash?:string};return{...j,network:this.chainId}}}export function getLedger():LedgerAdapter{const rpc=process.env.STRATUM_CHAIN_RPC_URL;return rpc?new StratumChainRpcAdapter(rpc,process.env.STRATUM_CHAIN_ID||'stratum-devnet-1',process.env.STRATUM_CHAIN_API_KEY):new MockStratumDevnetAdapter()}