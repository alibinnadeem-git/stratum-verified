export type LifecycleEventType='REGISTER_ASSET'|'PROCURE'|'SHIP'|'TRANSFER_CUSTODY'|'RECEIVE'|'INSTALL'|'INSPECT'|'TEST'|'COMMISSION'|'ENERGIZE'|'MAINTAIN'|'REPAIR'|'REPLACE'|'DECOMMISSION';export type LedgerRecord={organizationId:string;projectId:string;assetId:string;recordId:string;type:LifecycleEventType;evidenceHash:string;timestamp:string;signer:string;payloadHash?:string};export type AnchorReceipt={network:string;txHash:string;blockHeight:number;timestamp:string};export interface LedgerAdapter{anchor(record:LedgerRecord):Promise<AnchorReceipt>;verify(recordId:string,evidenceHash:string):Promise<{valid:boolean;network:string;blockHeight?:number;txHash?:string}>}export class MockStratumDevnetAdapter implements LedgerAdapter{async anchor(record:LedgerRecord){const seed=[...record.recordId].reduce((a,c)=>a+c.charCodeAt(0),0);return{network:'stratum-devnet-1',txHash:`stratum:dev:${seed.toString(16)}${record.evidenceHash.slice(0,8)}`,blockHeight:8313000+(seed%1000),timestamp:new Date().toISOString()}}async verify(recordId:string,evidenceHash:string){return{valid:Boolean(recordId&&evidenceHash),network:'stratum-devnet-1',blockHeight:8312871,txHash:'stratum:dev:verified-demo'}}}export const ledger:LedgerAdapter=new MockStratumDevnetAdapter();